<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Database Results</h2>

<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.id %> -  <img src="<%= r.url %>" alt="meme" height="100" width="100"></li>
    <% }); %>
</ul>

</div>

</body>
</html>
